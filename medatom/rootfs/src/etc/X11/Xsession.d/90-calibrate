CALIBRATION_FILE=/etc/X11/xorg.conf.d/99-calibration.conf

if ! test -f "$CALIBRATION_FILE"; then
	mount -o remount,rw /persist
	evdev-calibrate --output "$CALIBRATION_FILE" && sync
	mount -o remount,ro /persist
fi
