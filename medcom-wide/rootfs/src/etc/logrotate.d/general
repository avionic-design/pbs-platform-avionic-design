/var/log/*.log /var/log/messages {
	daily
	missingok
	notifempty
	maxsize 1M
	rotate 1
	compress
	sharedscripts
	postrotate
		/usr/bin/systemctl reload-or-try-restart syslog.service || true
	endscript
}
