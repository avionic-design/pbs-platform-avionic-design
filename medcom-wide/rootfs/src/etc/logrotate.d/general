/var/log/*.log /var/log/messages {
	daily
	missingok
	notifempty
	maxsize 4M
	rotate 7
	compress
	sharedscripts
	postrotate
		/usr/bin/kill -HUP `cat /var/run/rsyslogd.pid 2> /dev/null` 2> /dev/null || true
	endscript
}
